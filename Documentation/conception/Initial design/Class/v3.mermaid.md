```mermaid
classDiagram
class Entry{
    <<abstract>>
    #/Uid: String
    #/Login: String
    #/Password: String
    #/App: String
    #/Note: String
}
class AbstractUser{
    <<abstract>>
    #userEntries: List~Entry*~
    +GetEntries()
    +AddEntry(entry Entry)
    +RemoveEntry(entry Entry)
}
AbstractUser o-- Entry : userEntries
class LocalUser
class OnlineUser{
    -Email: String
    +/MasterPassword: String

    +GetEmail()
    +SetEmail(email: String)
}

class ProprietaryEntry{
    -SharedWith*: AbstractUser

    +GetSharedWith()
    +AddSharedUser(user: AbstractUser)
    +RemoveSharedUser(user: AbstractUser)
}
Entry <|-- ProprietaryEntry

class SharedEntry{
    +/Owner: final AbstractUser
}

SharedEntry --> "* #owner" OnlineUser
Entry <|-- SharedEntry

%%classes that use ProprietaryEntry
Entry <|-- AbstractUser
AbstractUser <|-- LocalUser
AbstractUser <|-- OnlineUser

class ReadonlyOnlineUserProxy{
    -final proxiedUser: OnlineUser

    +Uid(): String;
    +Login(): String;
    +Password(): String;
    +App(): String;
    +Note(): String;
    +Email(): String;
    +Entries(): List<Entry>;
}

OnlineUser "final managed" <-- ReadonlyOnlineUserProxy

class EntryFactory{
    <<abstract>>
    +createEntry(login, password, url, note)
}
Entry <.. EntryFactory
```
---
```mermaid
classDiagram

class Manager{
    +GetEntry(position: int)
    +AddEntry(entry: Entry)
    +GetUserLogged()
}

User "* userLogged"<-- Manager

```
---
```mermaid
classDiagram

Authenticator <|-- LocalOnlyAuthenticator
Authenticator <|-- DataBaseAuthenticator
class Authenticator{
    <<interface>>
    bool Authenticate(login, password)
}
class LocalOnlyAuthenticator{
    - bool CheckPassword(password)
}

class Encrypter {
    <<interface>>
    string Encrypt(key: String, entry: Entry)
}
class Decrypter{
    <<interface>>
    string Decrypt(key: String, entry: Entry)
}

Encrypter <|-- AESEncrypter
Decrypter <|-- AESDecrypter
```

note: add autocompletion